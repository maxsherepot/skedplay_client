interface Status {
    status: Boolean!
    message: String
}

type StatusPayload implements Status {
    status: Boolean!
    message: String
}

type Query {
    me: User @auth(guard:"api")
}

input AgeRange {
    from: Int!
    to: Int!
}
