type Service {
    id: ID!
    name: String!
    pivot: ServicePivot
    group_id: Int
    groupService: GroupService @hasOne
}

type ServicePivot {
    price: String
}

input ServiceInput {
    id: String
    price: String
}

extend type Query {
    services: [Service!]! @all
}
