type HelpCenterCategoryAllLangs {
    id: Int!
    name: String!
    active: Int!
    topics(active: Int @eq): [HelpCenterTopicAllLangs!] @hasMany(relation: "topics")
}

extend type Query {
    helpCenterCategories(active: Int @eq): [HelpCenterCategoryAllLangs!]! @all
}
